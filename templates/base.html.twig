<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}SpartaMGym - Gestión de gimnasio{% endblock %}
		</title>
		<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🏋️</text></svg>">
		{% block stylesheets %}
			<link rel="stylesheet" href="{{asset('build/css/bootstrap.min.css')}}">
			<link rel="stylesheet" href="{{asset('build/css/bootstrap-icons/font/bootstrap-icons.css')}}">
			<link rel="stylesheet" href="{{asset('build/css/bootstrap-table.min.css')}}">
			<link rel="stylesheet" href="{{asset('build/css/sweetalert2.min.css')}}">
            <style>
                @import url('https://fonts.googleapis.com/css2?family=Sora:wght@400;500;600;700;800&display=swap');
                :root {
                    --ink-950: #0b1220;
                    --ink-900: #122036;
                    --ink-800: #1e293b;
                    --ink-700: #334155;
                    --ink-500: #64748b;
                    --surface-0: #ffffff;
                    --surface-1: #f8fbff;
                    --surface-2: #e2ebf8;
                    --brand-500: #2563eb;
                    --brand-600: #1d4ed8;
                    --accent-500: #0891b2;
                    --ok-500: #059669;
                    --warn-500: #d97706;
                    --danger-500: #dc2626;
                    --radius-lg: 14px;
                    --radius-xl: 20px;
                    --shadow-soft: 0 14px 36px -28px rgba(15, 23, 42, 0.45);
                    --shadow-strong: 0 26px 54px -36px rgba(15, 23, 42, 0.65);
                }
                * {
                    box-sizing: border-box;
                }
                body {
                    min-height: 100vh;
                    background:
                        radial-gradient(circle at 8% 14%, rgba(59, 130, 246, 0.14) 0%, transparent 34%),
                        radial-gradient(circle at 87% 84%, rgba(6, 182, 212, 0.13) 0%, transparent 35%),
                        linear-gradient(160deg, #f9fbff 0%, #f4f7ff 48%, #f1f8ff 100%);
                    color: var(--ink-800);
                    font-family: 'Sora', 'Segoe UI', 'Helvetica Neue', sans-serif;
                }
                .navbar-custom {
                    background: rgba(15, 23, 42, 0.9);
                    backdrop-filter: blur(8px);
                    border-bottom: 1px solid rgba(148, 163, 184, 0.24);
                    box-shadow: 0 20px 42px -30px rgba(15, 23, 42, 0.88);
                }
                .navbar-brand.fw-bold {
                    letter-spacing: -0.5px;
                }
                .navbar-custom .nav-link {
                    font-weight: 500;
                    color: rgba(255, 255, 255, 0.74) !important;
                    transition: color 0.2s ease, background-color 0.2s ease, transform 0.2s ease;
                    border-radius: 999px;
                    padding-inline: 0.95rem !important;
                }
                .navbar-custom .nav-link:hover,
                .navbar-custom .nav-link:focus-visible {
                    color: #ffffff !important;
                    background: rgba(255, 255, 255, 0.08);
                    transform: translateY(-1px);
                }
                .navbar-custom .dropdown-menu {
                    border-radius: 14px;
                    border: 1px solid #dbe6f4;
                    padding: 0.5rem;
                }
                .navbar-custom .dropdown-item {
                    border-radius: 10px;
                    padding-inline: 0.7rem;
                }
                .navbar-custom .dropdown-item:hover {
                    background: #eff6ff;
                }
                .navbar-custom .dropdown-divider {
                    margin-inline: 0.3rem;
                }
                .btn-rounded {
                    border-radius: 999px;
                }
                .card-custom {
                    border: none;
                    border-radius: var(--radius-xl);
                    box-shadow: var(--shadow-soft);
                }
                .card-custom:hover {
                    box-shadow: var(--shadow-strong);
                }
                .page-header {
                    border-radius: 0 0 30px 30px;
                    box-shadow: 0 26px 44px -34px rgba(15, 23, 42, 0.8);
                }
                .table-container,
                .card-custom,
                .app-surface {
                    background: rgba(255, 255, 255, 0.93);
                    border: 1px solid rgba(226, 232, 240, 0.9);
                    backdrop-filter: blur(6px);
                }
                .form-control,
                .form-select,
                .input-group-text {
                    border-radius: var(--radius-lg);
                    border-color: #d4deec;
                }
                .form-control:focus,
                .form-select:focus {
                    border-color: var(--brand-500);
                    box-shadow: 0 0 0 0.2rem rgba(37, 99, 235, 0.14);
                }
                .btn-primary {
                    background: linear-gradient(120deg, var(--brand-600) 0%, var(--brand-500) 100%);
                    border-color: transparent;
                }
                .btn-primary:hover {
                    background: linear-gradient(120deg, #1e40af 0%, #1d4ed8 100%);
                    border-color: transparent;
                }
                .table {
                    --bs-table-bg: transparent;
                }
                .table > :not(caption) > * > * {
                    border-bottom-color: #e2e8f0;
                }
                .table-hover > tbody > tr:hover > * {
                    --bs-table-accent-bg: #f0f7ff;
                }
                .badge {
                    font-weight: 600;
                }
            </style>
			<script src="{{asset('build/js/jquery.min.js')}}"></script>
			<script src="{{asset('build/chart.js/chart.umd.js')}}"></script>
			<script src="{{asset('build/js/utils.js')}}"></script>
		{% endblock %}

	</head>
	<body>
        <nav class="navbar navbar-expand-lg navbar-dark navbar-custom sticky-top py-3">
            <div class="container">
                <a class="navbar-brand fw-bold d-flex align-items-center" href="{{ path('app_panel') }}">
                    <i class="bi bi-lightning-charge-fill text-primary me-2"></i>
                    <span>SpartaM<span class="text-primary">Gym</span></span>
                </a>
                <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto gap-2">
                        <li class="nav-item">
                            <a class="nav-link px-3" href="{{ path('app_panel') }}">
                                <i class="bi bi-grid-1x2-fill me-1"></i> Panel
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link px-3" href="{{ path('app_ingreso') }}">
                                <i class="bi bi-arrow-left-right me-1"></i> Control de acceso
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

		{% block body %}{% endblock %}

		{% block javascripts %}
			<script src="{{asset('build/js/bootstrap.bundle.min.js')}}"></script>
			<script src="{{asset('build/js/bootstrap-table.min.js')}}"></script>
			<script src="{{asset('build/js/sweetalert2.all.min.js')}}"></script>
		{% endblock %}
	</body>
</html>
